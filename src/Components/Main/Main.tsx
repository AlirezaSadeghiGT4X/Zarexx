import Prices from "./Prices";

export default function Main() {
    return (
        <main className="flex-1 bg-white dark:bg-neutral-900 px-4 py-4 sm:px-20 flex items-center justify-center transition-all duration-500">
            <Prices />
        </main>
    )
}